[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rehabnet-classic"
version = "0.1.0"
description = "Classic ST-GCN + Transformer + BiLSTM RehabNet pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

